pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'docker build -t maskwebsite/makswebsite:latest .'   
            }
        }
        stage('Push') {
            steps {
                withCredentials([string(credentialsId: 'maks2531', variable: 'dockerpwd')]) {
                    sh 'echo "${dockerpwd}" | docker login --username maks2531 --password-stdin'
                    sh "docker push maks2351/makswebsite:latest"
                }
            }
        }
        stage('Clean') {
            steps {
                sh 'docker rmi -f $(docker images -aq)'
            }
        }
    }
}



