pipeline {
    agent { label 'master' }

    stages {
        stage('Build') {
            steps {
                sh 'docker image ls'
                sh 'docker build -t makswebsite .'
            }
        }
        stage('test') {
            steps {
                sh 'docker image ls'
                sh 'docker tag makswebsite maks2531/makswebsite:$BUILD_NUMBER'
            }
        }
        stage('Push') {
            steps {
                withCredentials([string(credentialsId: 'maks2531', variable: 'dockerpwd')]) {
                    sh 'echo "${dockerpwd}" | docker login --username maks2531 --password-stdin'
                    sh 'docker push maks2531/makswebsite:$BUILD_NUMBER'
                }
            }
        }
    }
post { 
        always { 
            sh 'docker kill $(docker ps -q)'
            sh  'docker rmi -f $(docker images -aq)'
        }
    }
}
