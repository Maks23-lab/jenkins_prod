pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'docker build -t maks2531/makswebsite .'   
            }
        }
        stage('Push') {
            steps {
                withCredentials([string(credentialsId: 'maks2531', variable: 'dockerpwd')]) {
                    sh 'echo "${dockerpwd}" | docker login --username maks2531 --password-stdin'
                    sh "docker push maks2351/makswebsite"
                }
            }
        }
        stage('Clean') {
            steps {
                sh 'docker rmi -f $(docker images -aq)'
            }
        }
    }
}



